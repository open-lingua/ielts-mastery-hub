# Optional override — if unset, the app resolves an OS-specific default
# (e.g. macOS: ~/Library/Application Support/com.openlingua.ieltsmasteryhub/imh.db)
DATABASE_URL=sqlite:dev.db

# Set to "true" to auto-open DevTools on app start
OPEN_DEVTOOLS=false
