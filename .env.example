# AI provider credentials (API keys, endpoints) are no longer configured via
# environment variables. Configure them from the app's Admin → AI Model
# Configurations page; they are stored encrypted in the local SQLite database.
